<h1>Playlists#index</h1>
<p>Find me in app/views/playlists/index.html.erb</p>

<h1>Sets</h1>
<ul>
  <% @sets.each do |set| %>
  <li> <%= link_to set.name, playlist_path(set) %> </li>
  <% end %>
</ul>

<h1>Playlists</h1>
<ul>
  <% @playlists.each do |playlist| %>
  <li> <%= link_to playlist.name, playlist_path(playlist) %> </li>
  <% end %>
</ul>

<%= link_to "new set", new_playlist_path %>
